# 判定結果 — siota-1 / V6 認証・V7 セッション管理・V9 中身入りの引換券

| | |
|---|---|
| 対象アプリ | `rahiseko-alt/siota-1`（SALTY DOG・ドッグトリマー向けのカルテ） |
| 対象の版 | `55c72a9a2ea6dc23074e59ae21643a4edb1e9e73`（master・2026-09-14）。判定後に master は `5995979` へ進んだが、進んだ分は記録の追記だけで、この23項目の判定に影響しない |
| 当てた固定表 | `../../table/V6.md`（13項目）／`../../table/V7.md`（6項目）／`../../table/V9.md`（4項目） |
| 判定日 | 2026-09-15 |

## 集計

| 判定 | 件数 |
|---|---|
| 合格 | 21（うち該当機能なし 11） |
| **不合格** | **2** |
| 判定不能 | **0** |
| **合計** | **23 / 23** |

判定のしかたの内訳: `機械` **5件**（6.2.6 / 6.2.7 / 6.4.2 / 9.1.2 / 9.1.3）、`読む` **18件**。

判定結果の見出しの数（23）が固定表の項目数（23）と一致している。この3章の確認は**終わっている**。

## 判定不能: 0件

**この3章に判定不能は残っていない。** 掘り下げヒアリングを2周して、12件あった判定不能を
すべて解消した。内訳は、8件が設定の確認（`D-20260915-77`）、2件が総当たりへの守りの
文書化（`D-20260915-78`）、2件が不合格としての確定（7.2.4 と 7.4.1）である。

## 掘り下げヒアリングで何が起きたか

この3章の判定は、**掘り下げヒアリングを通したことで大きく動いた**。

**パスワードの8項目（6.2.1〜6.2.8）が、判定不能から合格（該当機能なし）へ。**
ただしその過程で、**決めたことと実際が食い違っていた**ことが分かった。

1. ヒアリングでマスターが「ログインの入り口は Google だけ。他は許さない」と決めた
2. その決定を裏づけるため、ログインの仕組みの側の設定を確認した
3. **Email provider が有効（Enabled）のままだった。** 画面を通さずにメールアドレスと
   パスワードで誰でも登録・ログインできる状態だった
4. マスターが無効（Disabled）に切り替えた。切る前に、このアプリがメールログインも
   メール送信も使っていないことをコードで確認している
5. 経緯を対象アプリの記録に書き戻した（`docs/decisions.md` の `D-20260915-77`）

**開いていたことで起こりえたこと**: カルテは見られない（勝手に登録した利用者はどの店にも
飼い主にも紐づかず、行単位のアクセス制御で一切のデータに到達できない）。起こりえたのは、
見知らぬアカウントが際限なく作られ、確認メールの送信枠を使い切らせることだった。

**この発見は、合格基準の書き方が生んだものである。** パスワードの8項目に
「画面にパスワード欄が無いだけでは該当しないと扱わない。ログインの仕組みの側が
受け付けないことが記録にあって初めて該当しないとする」と**あらかじめ書いておいた**ため、
判定不能が残り、その解消が設定の確認につながった。基準を緩めていれば、
「画面に無いから該当しない」で通り過ぎていた。

**総当たりへの守りの2項目（6.1.1・6.3.1）も、判定不能から合格へ。** 持ち主が
`Attack Protection` の画面を確認し、その実測値とともに守りの方針を1か所に文書化した
（`D-20260915-78`）。ASVS 6.1.1 が求めているのは守りの強さではなく**文書があること**なので、
書いたことで合格になる。**Captcha が無効である**という事実は合格の中に含まれている。

**不合格2件（7.2.4 と 7.4.1）**も、同じヒアリングの中で確定した。ログイン状態の扱いを
公式資料で調べることの承認を得て調べた結果、判定不能だったもの（7.2.4）と、基準の欠陥で
合格としていたもの（7.4.1）が、どちらも不合格だと分かった。

---

## V6 認証（13項目）

### v5.0.0-6.1.1 — 総当たり対策が文書に定められているか

**判定: 合格**

根拠: `docs/decisions.md` の `D-20260915-78`

合格基準の2点をどちらも満たす。

1. **ログインへの総当たりをどう防いでいるか**: 入り口が Google だけであり、このアプリは
   パスワードを持たないため、当てにいく対象が存在しない。守りは Google 側にあると明記
   されている。アプリ側に独自の回数制限を作らない方針と、その理由（二重に作ると片方だけ
   直して食い違う）も書かれている。
2. **正規の利用者が締め出されないようにする仕組み**: 締め出しが起きるのは Google 側で
   あり、このアプリが誰かを締め出すことはないと明記。締め出されたときは別の端末や別の
   ネットワークから入り直し、それでも駄目なら時間を置く、と手順が書かれている。アプリ側に
   締め出す仕組みを作らない理由（なりすましによる嫌がらせを成立させない）も書かれている。

**設定の実測も記録に含まれている**（2026-09-15・持ち主が画面を確認）。

| 設定 | 状態 |
|---|---|
| Captcha protection | 無効 |
| 漏洩パスワードの拒否 | 無効（パスワードでのログインを切ったため意味を持たない） |
| Email provider | 無効 |
| 匿名ログイン | 無効 |
| 新規登録の許可 | 有効（Google での初回ログインに必要） |

**正直に書いておくこと**: この記録は、判定不能を解消するために**この安全確認の中で
書かれたもの**である。内容は持ち主の決定（`D-20260915-75`）と、画面で確かめた実測値から
成っており、事実に反する記述は含まない。ただし「文書があるから合格」であって、
「守りが強いから合格」ではない。**Captcha が無効である**という事実は、合格の中に含まれて
いる。利用者が増えて自動的な大量登録が実際に起きたら有効にする、という見直す条件も記録に
書かれている。

### v5.0.0-6.2.1 — パスワードが8文字以上か

**判定: 合格（該当機能なし）**

### v5.0.0-6.2.2 — 利用者が自分でパスワードを変更できるか

**判定: 合格（該当機能なし）**

### v5.0.0-6.2.3 — 変更にいまのパスワードを求めるか

**判定: 合格（該当機能なし）**

### v5.0.0-6.2.4 — よく使われるパスワードを弾くか

**判定: 合格（該当機能なし）**

### v5.0.0-6.2.5 — 文字種を強制していないか

**判定: 合格（該当機能なし）**

### v5.0.0-6.2.6 — パスワード入力欄が伏せ字か

**判定: 合格（該当機能なし）**

### v5.0.0-6.2.7 — 貼り付けや自動入力を妨げていないか

**判定: 合格（該当機能なし）**

### v5.0.0-6.2.8 — パスワードに手を加えずそのまま照合するか

**判定: 合格（該当機能なし）**

#### パスワード8項目に共通の根拠

固定表の「該当しない条件」——**共通の前提の2条件をどちらも満たすこと**——を、2026-09-15 の
掘り下げヒアリングの結果として満たした。

**条件1（【パスワード検索】の出力が0件）— 満たす。** 命令の出力は1件だが、その行を読んで
確かめた結果、**手元での試験専用の道具**だった（`scripts/lib/local-stack.mjs:1-8` に、手元の
環境を起動して検証するための共通部品であり本番の鍵を使わないと明記。`:105-106` に
「ログイン画面は Google 認証のみを表示するので、これは試験専用の裏口」と明記）。本番の画面
から到達できないため、固定表の但し書きの規則により数に入れない。

```
./scripts/lib/local-stack.mjs:108: type=password
```

**条件2（ログインの仕組みの側がパスワードでのログインを受け付けないことが記録にある）—
満たす。** `docs/decisions.md` の `D-20260915-77` に記録された。

確認の経緯: ヒアリングの時点では **Email provider が有効（Enabled）のまま**で、画面を
通さずに登録・ログインできる状態だった。マスターが無効（Disabled）に切り替え、その事実を
記録に書き戻した。切る前に、このアプリがメールを使ったログインもメール送信も一切使って
いないことをコードで確認している（`signInWithOtp` / `magiclink` / `verifyOtp` /
メール送信のいずれも0件。招待はQRコードでメールを介さない）。

**この判定の限界**: `D-20260915-77` は**マスターの操作報告**であり、こちらが画面を見て
確かめたものではない（本番の設定には触れない決まりのため）。再確認のしかたは同記録に
書いてある。設定が変われば、この8項目の判定も変わる。

### v5.0.0-6.3.1 — 総当たり対策が文書のとおり実装されているか

**判定: 合格**

6.1.1 が合格になったため、比べる相手ができた。文書（`D-20260915-78`）が述べている守りが、
実装として存在することを確かめた。

| 文書が述べていること | 実装 |
|---|---|
| 入り口は Google だけ | `backend/js/supabase-auth.js:62-76` の `signInWithGoogle` が唯一の経路。Email provider は無効（`D-20260915-77`） |
| アプリ側に独自の回数制限を作らない | ログインを守る回数制限の実装は存在しない。`public.consume_rate_limit` はログイン済みの利用者の操作に対するもので、実行権限もログイン済みの利用者にしか与えられていない（`supabase/migrations/202607160001_supabase_base.sql:728`） |
| 匿名ログインを使わない | 無効（実測）。ローカルの設定でも `enable_anonymous_sign_ins = false`（`supabase/config.toml`） |

文書と実装が食い違っている箇所は無い。

### v5.0.0-6.3.2 — 初めから用意された利用者アカウントが無いか

**判定: 合格**

1. 本番に適用される定義（`supabase/migrations/` の14ファイル）の中に、利用者アカウントを
   作る処理は無い。
2. 試験用のアカウントを作るのは `supabase/seed.sql` のみで、手元で環境を起動したときだけ
   読み込まれる。手元専用であることが `scripts/lib/local-stack.mjs:1-8` と
   `docs/decisions.md:168` に書かれている。

`admin@local.test` という紛らわしい名前のアカウントは、権限を1つに統合した際に削除されて
いる（`supabase/seed.sql:45`）。

### v5.0.0-6.4.1 — 仕組みが発行する初期パスワードや有効化コードが安全か

**判定: 合格**

招待コードが該当する。合格基準の3点をすべて満たす。

1. **安全な乱数**: `encode(extensions.gen_random_bytes(32), 'hex')`
   （`supabase/migrations/202607160001_supabase_base.sql:390`）。32バイト＝256ビットで、
   必要な水準を超えている。時刻や連番ではない。コード自体は保存されず、`sha256` で
   変換した値だけが保存される（同 `:392` 付近）。
2. **有効期限がある**: 引き換え時に `candidate.expires_at > now()` を満たさなければ弾かれる
   （`supabase/migrations/202607160003_invitation_management.sql:22`）。
3. **一度使われたら二度目は通らない**: `claimed_at is null` を条件に取り出し、取り出した
   直後に `claimed_at` を埋める。同時に2人が押しても片方しか通らない
   （同 `:20, :29-34`）。

### v5.0.0-6.4.2 — パスワードのヒントや秘密の質問が無いか

**判定: 合格**

命令の出力: **0件**。

---

## V7 セッション管理（6項目）

### v5.0.0-7.2.1 — ログイン引換券の検証がサーバ側で行われているか

**判定: 合格**

ブラウザ側の JavaScript が引換券の中身を自前で解いて「この人は誰か」を決めている箇所は
無い。検証は2か所のサーバ側で行われている。

- 中継の Cloudflare Worker が、受け取った引換券をそのままログインの仕組みに問い合わせて
  確かめる（`worker/src/auth-context.js:44`）。返事が芳しくなければ 401 で止める（同 `:54`）。
- データベース側も、行単位のアクセス制御の中で利用者を判定している。

命令の出力に `backend/js/supabase-storage.js:47` の `atob` が出たが、その行を読んだところ
**画像データの変換**（JPEG/PNG/WebP の base64 を戻す処理）であり、引換券とは無関係だった
（`backend/js/supabase-storage.js:44-50`）。

### v5.0.0-7.2.2 — その都度作られる引換券を使っているか

**判定: 合格**

利用者の見分けには、ログインのたびに発行される引換券を使っている
（`worker/src/auth-context.js:39` で受け取り、`:44` で確かめる）。

ブラウザに固定の鍵（`SUPABASE_PUBLISHABLE_KEY`）を置いているが、これは**公開してよい鍵**で
あり、利用者の見分けには使っていない。この鍵だけではデータに到達できず、別に引換券が要る
ことは、行単位のアクセス制御の判定がすべて利用者の身元に依存していることから確かめられる
（V8 の判定結果を参照）。設定の見本にも公開用の鍵として記載がある（`.env.example:4`）。

### v5.0.0-7.2.3 — 引換券が安全な乱数で作られているか

**判定: 合格（該当機能なし）**

対象アプリ自身がログイン引換券を作っている箇所は無い。命令の出力は次のとおり。

| 出たもの | 件数 | 用途（読んで確かめた） |
|---|---:|---|
| `gen_random_bytes` | 3ファイル | 招待コードの生成（6.4.1 で判定済み） |
| `randomUUID` | 2件 | 画像ファイルの名前の生成（`backend/js/supabase-storage.js:63, 71`） |
| `Math.random` | **14件** | すべて `scripts/verify-*.mjs`（手元の検証用の道具）の中。本番に配られるコードではない |

**`Math.random` が0件ではない点を明記する。** 予測しやすい乱数が14か所で使われているが、
いずれも手元の検証用であり、ログイン引換券の生成には使われていない。合格基準が問うのは
「ログイン引換券の生成に使っていないか」であり、それは満たす。

ログインを外部の仕組みに任せていることは、`backend/js/supabase-auth.js:62-76` と
`worker/src/auth-context.js:44` から特定できる。

### v5.0.0-7.2.4 — ログインのたびに引換券が入れ替わるか

**判定: 不合格**

対象アプリ自身は引換券を作っていないため、入れ替えの挙動は任せ先が決めている。
掘り下げヒアリングでの承認を得て公式資料を調べた結果、**既定では前のログイン状態が
終わらない**ことが分かった。

> 既定では、1人の利用者が何台の端末でも、いくつでも同時にログイン状態を持てる
>
> — https://supabase.com/docs/guides/auth/sessions

「1人につき1つだけ許す」設定は存在するが、上位の契約が必要で、既定では無効。しかも
切り替わりは次回の更新時なので即座ではない。

ログインし直しても新しいログイン状態が足されるだけで、前のものは並行して生き続ける。
合格基準の「古いものが使えなくなること」を満たさない。

調べた内容は対象アプリの記録に書き戻した（`docs/decisions.md` の `D-20260915-76`）。
次回からこの項目は記録だけで判定できる。

**どうすると直るか**: 上位の契約に切り替えて「1人につき1つだけ」を有効にする。ただし
切り替わりが即座でない点は残る。

### v5.0.0-7.4.1 — ログアウト後に引換券が使えなくなるか

**判定: 不合格**

**この項目は、いったん合格と判定したものを訂正した。** 合格基準が ASVS の原文を写し取れて
おらず、穴を見逃していた（`../../../adr/0005`）。

合格基準の1点目は満たす。ログアウトは既定で全端末に効き、サーバ側のログイン状態の行が
削除される。記録にも明記がある（`docs/ops/proof-of-red.md:2762-2763`）。実装の裏づけも
公式にある（https://github.com/supabase/auth-js/blob/master/src/GoTrueClient.ts）。

**2点目を満たさない。** 発行済みの通行証（中身入りの引換券）は取り消せない。

> 利用者の通行証を、期限が来る前に取り消す方法は無い
>
> — https://github.com/supabase/auth-js/blob/master/src/GoTrueClient.ts

既定の有効期間は1時間（https://supabase.com/docs/guides/auth/sessions）。つまり
**ログアウトしたあとも、最大1時間はその通行証でデータを読める**。ASVS が求める手当て
（無効にしたものの一覧を持つ、利用者ごとの日時より前のものを弾く等）は、このアプリには無い。

**影響の範囲**: 退職・退会の場面では別の守りが効いている。`shop_memberships.active` と
`owners.active` の確認がアクセス制御に入っているため、通行証が生きていてもデータには
到達できない（7.4.2 は合格）。**残る穴は、本人がログアウトした直後の1時間**である。
端末の紛失・共用端末での操作といった場面で意味を持つ。

**どうすると直るか**: 通行証の中の `session_id` が、サーバ側に残っているログイン状態と
一致するかをアクセス制御の条件に足す。公式が案内している方法である
（https://supabase.com/docs/guides/auth/sessions の FAQ「How to ensure an access token
(JWT) cannot be used after a user signs out」）。通行証の有効期間を1時間より短くすると、
危険な時間はその分だけ縮む。

調べた内容と直しかたは、対象アプリの記録に書き戻した（`docs/decisions.md` の
`D-20260915-76`）。

### v5.0.0-7.4.2 — 利用者を無効にしたとき、到達できなくなるか

**判定: 合格**

お店の人・飼い主の両方について、無効化したときに**すべての経路**で到達が止まる。

**お店の人**: 判定は `private.is_shop_staff` の**1本に統合されている**。この関数が
`membership.active` を確かめる（`supabase/migrations/202609060012_single_staff_role.sql:27-39`）。
かつて `is_shop_admin` という2本目があったが、権限を1つにした際に廃止され、それを見ていた
アクセス制御はすべて `is_shop_staff` に付け替えられた（同 `:6-11, :66, :76-95`）。
判定が1本しかないため、経路の抜けが構造的に起きない。

**飼い主**: 判定は4本あり、そのすべてに `owners.active` の確認が入っている
（`supabase/migrations/202607160007_owner_access_control.sql:47`／`:64-65`／`:85-86`／
`:105-110`）。この4本は、かつて**1本だけ抜けていて「退会済みに見えるのに見えている」
状態だった**ことが記録されており、その修正として4本に揃えられた
（同 `:6-12`、`docs/decisions.md:360`）。

**明記しておくこと**: 引換券そのものは失効させていない。無効にした利用者の画面は、見かけ上
はログインしたままになる。ただしデータには一切到達できない。合格基準は到達の可否で判定すると
定めているため合格としたが、この事実は隠さず記録する。

---

## V9 中身入りの引換券（4項目）

対象アプリは、引換券の中身を自前で解いたり作ったりしていない。命令の出力で自前の検証は
0件だった（`atob` の1件は画像の変換で、7.2.1 で確認済み）。

### v5.0.0-9.1.1 — 中身を信じる前に署名を確かめているか

**判定: 合格**

自前で検証していないため、合格基準の2番目で判定した。**検証を行っている先を名指しできる。**
中継の Worker が `/auth/v1/user` に問い合わせて確かめており（`worker/src/auth-context.js:44`）、
確かめられなければ 401 で止める（同 `:54`）。「たぶん任せている」ではなく、コードで追える。

### v5.0.0-9.1.2 — 「署名なし」を受け入れる指定が無いか

**判定: 合格（該当機能なし）**

命令の出力: **0件**。加えて自前の作成・検証が0件のため、該当機能なしとした。

### v5.0.0-9.1.3 — 引換券が指定してきた鍵の出どころを信じていないか

**判定: 合格（該当機能なし）**

命令の出力: **0件**。加えて自前の検証が0件のため、該当機能なしとした。

### v5.0.0-9.2.1 — 有効期間を確かめているか

**判定: 合格**

自前で検証していないが、確かめている先を特定できる（`worker/src/auth-context.js:44`。
期限切れの引換券はこの問い合わせで弾かれる）。9.1.1 と同じ根拠・同じ形で判定している。

---

## この判定の限界（正直に記録する）

- **本番で動いている設定を、こちらの目では見ていない。** 見たのはコードと、手元用の設定
  ファイルと、経緯の記録だけである。設定の実測値は**持ち主の操作報告**として記録に
  書き戻されたものであり、こちらが画面を見て確かめたものではない（本番の設定には触れない
  決まりのため）。設定が変われば判定も変わる。再確認のしかたは記録に書いてある。当初12件あった判定不能のうち10件は、まさにこの限界から出ていた。持ち主が画面を確認し、その結果を記録に書き戻したことで解消した。
- `機械` の5項目は何回実行しても同じ結果になる。`読む` の18項目は文章の判断が入るため、
  完全な再現は保証しない。見る場所を命令で絞ることで揺れを抑えている。
- 7.4.2 は、引換券を失効させていないという事実を含んだうえでの合格である。合格基準を
  「到達できないこと」と定めた結果であり、基準を変えれば判定も変わる。
