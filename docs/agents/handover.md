# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-15 決めたことと実際の食い違いを見つけて閉じた（Issue #4 完了）

**決めたこと**

- マスターが「ログインの入り口は Google だけ」と決めた直後に設定を確認したところ、
  Email provider が有効のままだった。マスターが無効に切り替えた
- 切る前に、このアプリがメールログインもメール送信も使っていないことをコードで確認した
- 経緯を siota-1 の台帳に書き戻した（D-20260915-77）。PR #113 に含めて送信済み
- 判定の集計に数え間違いがあったので数え直した。以後、項目を機械で数えて表と突き合わせる

**判定の変化（V6/V7/V9 の23項目）**

- 合格19（うち該当機能なし11）/ 不合格2 / 判定不能2
- パスワード8項目: 判定不能 → 合格（該当機能なし）
- 判定不能は 12 → 2 に減った。残るのは 6.1.1 と 6.3.1 の2件だけ

**次にやること**

- マスターが Authentication → Attack Protection の画面を見せる。これで 6.1.1 と 6.3.1 が
  片付き、判定不能が0になる。Sessions の画面も見られれば 7.2.4 の直しかたが確定する
- `/implement` で #6〜#8（残り43項目）
- 最後に #9 で通し実行と3つの検査

**未解決の問題**

- 不合格2件（ログインし直しで前のログイン状態が終わらない／ログアウト後も通行証が
  最大1時間通る）を直すかどうかは siota-1 の持ち主の判断。Free プランでは上位の設定が
  使えないため、通行証の突き合わせを足す方法が現実的
- 設定の確認はマスターの操作報告に依るもので、こちらが画面を見て確かめてはいない

## 2026-09-15 掘り下げヒアリングを実地で通した（Issue #4）。不備が2件見つかった

**決めたこと**

- siota-1 への書き込み権限を取得し、ヒアリングの答えを向こうの台帳に書き戻した
  （docs/decisions.md の D-20260915-74/75/76）。下書きとして提出済み（siota-1 の PR #113）
- 書き戻しは追記のみ・向こうの書式に従う・作業用の枝に置く・直しは含めない、と運用手順に明記
- 7.4.1 の合格基準に欠陥があった。ASVS が明示している「取り消せない通行証の場合の手当て」を
  写し取れておらず、合格に倒していた。基準を直して判定し直した（docs/adr/0005）
- 基準は判定の途中では変えない。変えてよいのは標準を写し取れていないと分かったときだけで、
  そのときは影響する項目をすべて判定し直す

**判定の変化（V6/V7/V9 の23項目）**

- 合格12 / 不合格0 / 判定不能11 → 合格10 / **不合格2** / 判定不能10
- 7.2.4 判定不能 → 不合格（ログインし直しても前のログイン状態が終わらない）
- 7.4.1 合格 → 不合格（ログアウト後も発行済みの通行証が最大1時間通る）

**次にやること**

- マスターが Supabase の画面で Email provider の有無を確認し、結果を siota-1 の台帳に
  追記する。これが済むとパスワード8項目が一度に動く（判定不能10 → 2）
- `/implement` で #6〜#8（残り43項目）
- 最後に #9 で通し実行と3つの検査

**未解決の問題**

- Supabase 側の Email provider の状態が未確認（上記）
- 見つかった不備2件を直すかどうかは siota-1 の持ち主の判断。今回の範囲外

## 2026-09-15 #5 のレビュー指摘を反映（判定を1件訂正）

**決めたこと**

- 7.4.1 を判定不能から合格へ訂正した。記録に答えが書いてあるのに見落としていた
  （docs/ops/proof-of-red.md:2762-2763。ログアウトはサーバ側でも失効する）
- 7.2.3 の根拠を訂正。「Math.random は0件」と書いていたが実際は14件。いずれも手元の
  検証用で、ログイン引換券の生成には使われていないため判定は変わらない
- 7.4.2 にお店の人側の根拠を追加。判定が is_shop_staff の1本に統合されている
- 用語を分けた。多義だった「合言葉」を廃し、ログインの仕組み／ログイン引換券／
  中身入りの引換券を CONTEXT.md に追加
- 紛らわしい行が出たときの扱いを全章で統一した。読んで確かめた場合だけ数に入れない
- 最終: 合格12（うち該当機能なし3）/ 不合格0 / 判定不能11

**次にやること**

- `/implement` で #4（判定不能の掘り下げヒアリングと書き戻し）に着手する。
  最優先は「ログインの仕組みがパスワードでのログインを受け付けるか」。ここが分かると8件動く
- そのあと #6〜#8 で残り43項目
- 最後に #9 で通し実行と3つの検査

**未解決の問題**

- siota-1 への書き込み権限がまだ無い（#4 で取得する）
- 本番で動いている設定は見ていない。判定不能11件のうち8件はこの限界から出ている

## 2026-09-15 認証・セッション・トークンの23項目を判定（Issue #5）

**決めたこと**

- V6（13）・V7（6）・V9（4）の固定表を作り、siota-1 に当てた。70項目のうち27項目が済
- 結果は 合格11（うち該当機能なし4）/ 不合格0 / 判定不能12。行数は23で固定表と一致
- 判定不能12件のうち10件は「利用者を管理している仕組みの側で、メールとパスワードによる
  ログインが止めてあるか」という1点が未確認であることから出ている。ここが分かれば10件が動く
- パスワード8項目の共通の但し書きを固定表に置いた。画面にパスワード欄が無いだけでは
  「該当しない」と扱わない。仕組みの側で止めてあることが記録にあって初めて該当しないとする
- 合格基準はその場で緩めない。試験専用の道具1件が検索に出たため、条件を満たさないものとして
  判定不能のままにした

**次にやること**

- `/implement` で #4（判定不能の掘り下げヒアリングと書き戻し）に着手する。
  判定不能が12件出たので、ここで実地に試せる。最優先はパスワードの1点
- そのあと #6〜#8 で残り43項目（V1/V2/V3/V5、V4/V10、V11〜V15）
- 最後に #9 で通し実行と3つの検査

**未解決の問題**

- siota-1 への書き込み権限がまだ無い（#4 で取得する）
- 本番で動いている設定は見ていない。コードと記録だけで判定している

## 2026-09-15 認可4項目の安全確認を通した（Issue #3）

**決めたこと**

- ASVS 公式データの L1 は実測 70 項目。章ごとの内訳も作業一覧の数字と一致した
- 公式データを docs/safety-check/asvs-5.0.0-l1.csv に同梱した。版を固定し、外部に依存させない
- 日本語は外部のドラフト訳に頼らず自分で訳し、原文を併記する（docs/adr/0004）
- 固定表の欄名は CONTEXT.md に合わせて「合格基準」。「合格条件」は使わない
- 命令が何も見つけられなかったときは合格ではなく判定不能とする。黙って合格に倒さない
- `読む` の項目には「見る場所」を命令で絞って書く。検品プログラムが無い項目を赤にする
- siota-1 の認可4項目は 合格4 / 不合格0 / 判定不能0。判定結果の行数が固定表と一致

**次にやること**

- `/implement` で #5〜#8 を進める（互いに独立。どれからでもよい）
- #4（判定不能のヒアリングと書き戻し）は、判定不能が出た章から着手する。
  認可の章では判定不能が0件だったため、まだ実地で試せていない
- 最後に #9 で通し実行と3つの検査

**未解決の問題**

- siota-1 への書き込み権限がまだ無い（#4 で取得する）
- 動いているデータベースの実状は見ていない。移行ファイルの内容だけで判定している。
  Supabase 公式の点検機能を当てるには対象アプリの管理画面への接続が要る

