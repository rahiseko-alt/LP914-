# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-16 判定不能が0件になった。70項目の確認が完全に終わった

**決めたこと**

- マスターが「部品に弱点が見つかってから直すまでの期間」を決めた。重大は7日以内、
  それ以外は30日以内。危険の度合いは `npm audit` が付ける印をそのまま使い、人が都度
  決めない。siota-1 の台帳に書き戻した（D-20260916-80、PR #113 に含めて送信済み）
- これで 15.1.1・15.2.1 が判定不能から合格へ。**最終: 合格68（うち該当機能なし21）/
  不合格2 / 判定不能0**
- 15.2.1 の合格は「今日の時点で期限を過ぎたものが無い」という意味。期限つきの宿題が
  6件ある（重大5件は 2026-09-23、それ以外1件は 2026-10-16）。6件すべて wrangler が
  連れてくるもので、利用者の画面には出ない。利用者に届く部品の弱点は0件
- 判定結果に残っていた古い数字（機械29／読む41）を実測値（機械18／読む52）に直した
- 「経緯の記録がまだ既定の枝に入っていない」ことを判定の限界に明記した。取り込まれる
  までは、既定の枝だけを見た次の確認で同じ項目がまた判定不能になるため

**次にやること**

- siota-1 の PR #113 を取り込む。取り込まないとヒアリングの答えが既定の枝に載らない
- 残る不合格2件（7.2.4・7.4.1）を直すかはマスターの判断。どちらもログイン引換券の扱い
- 2026-09-23 までに wrangler を上げるかを決める。`npm audit fix --force` は package.json
  に書いた範囲の外へ上げるため、マスターの判断が要る

**未解決の問題**

- なし

## 2026-09-15 仕様書の「範囲外」を ADR 0007 に合わせて直した

**決めたこと**

- 仕様書（Issue #2）の「範囲外」にあった「対象アプリの外形的な検査はしない」を
  取り消し線にし、`docs/adr/0007-check-the-outside-too.md` で置き換わった旨を書いた。
  実際には公開されている画面へ応答を取りに行っており、記述と実態が食い違っていたため
- 置き換えた範囲の条件3つ（読むだけ／持ち主の許可があるものに限る／実行した命令と
  出力をそのまま残す）も仕様書側に書いた

**次にやること**

- 「部品に弱点が見つかったら、重大なものは◯日以内、それ以外は◯日以内に直す」の日数を
  マスターが決める。決まれば判定不能2件が0になる。文章はこちらで書く
- 残る不合格2件（7.2.4・7.4.1）を直すかはマスターの判断。どちらもログイン引換券の扱い

**未解決の問題**

- なし（上の2件はマスターの判断待ちであり、こちらで詰まっている箇所は無い）

## 2026-09-15 確認の最中に不備3件が直った。不合格2・判定不能2まで減った

**決めたこと**

- マスターが Cloudflare の設定を3つ変えた。Always Use HTTPS 有効化／HSTS 有効化（1年）／
  Minimum TLS Version を TLS 1.0 から 1.2 へ。コードは1行も変えていない
- 12.2.1（最も重い不備）と 3.4.1 は実測で直ったことを確認した。
  http は 301 で https へ転送、strict-transport-security: max-age=31536000
- 12.1.1 は判定不能から合格へ。根拠はマスターの操作報告（手元から古い版を試せないため）
- HSTS の includeSubDomains と Preload は意図的に無効のまま。やり直しが難しく L1 は
  求めていない。L2 に上げるときは 3.4.1 が再び不合格になることを判定結果に明記した
- 経緯を siota-1 の台帳に書き戻した（D-20260915-79）。PR #113 に含めて送信済み
- 最終: 合格66（うち該当機能なし21）/ 不合格2 / 判定不能2

**次にやること**

- 「部品に弱点が見つかったら、重大なものは◯日以内、それ以外は◯日以内に直す」の日数を
  マスターが決める。決まれば判定不能2件が0になる。文章はこちらで書く
- 残る不合格2件（7.2.4・7.4.1）を直すかはマスターの判断。どちらもログイン引換券の扱い
- 仕様書（Issue #2）の「範囲外」に docs/adr/0007 への参照を足す

**未解決の問題**

- 12.1.1 の根拠はマスターの操作報告に依る。設定が戻れば判定も戻る

## 2026-09-15 最終レビューの指摘を反映。規則を機械に見張らせた

**決めたこと**

- 検品プログラムに規則の強制を足した。`2>/dev/null` の禁止・対象アプリ固有の置き場所名の
  禁止・章の欠落・欄の重複を、すべて機械が赤にする。入れた瞬間に12件の違反を検出した
- `--results` を足し、判定結果の数と `機械`／`読む` の内訳を機械が数えるようにした。
  仕様が「目で数えない」と定めているのに数える道具が無かったため
- `機械` に人の判断が混ざっていた5項目（1.2.2・3.3.1・11.4.1・13.4.1・15.3.1）を
  正直に `読む` へ変えた。内訳は 機械18 / 読む52
- 1.2.5 の根拠が虚偽だった。「0件」と書いていたが実際は62行。命令の探す範囲を広げたのに
  実行し直していなかった。全件を読んで記録し直した（判定そのものは合格のまま）
- 判定した版を master の頭に揃え、章ごとに違っていた記載を直した
- 公開されている画面への応答を読むことを、範囲に入れると決めて記録した（docs/adr/0007）。
  仕様書は範囲外としていたが、実際には見ており、黙って広げたままにしないため
- マスターが Cloudflare の Always Use HTTPS を有効化。実測で 301 転送を確認した

**次にやること**

- マスターが HSTS を有効化（Max Age は1年以上）し、Minimum TLS Version を 1.2 に変える。
  終わったら実測して判定を更新する
- Minimum TLS Version が TLS 1.0 だったため、12.1.1 は判定不能から不合格に変わる見込み。
  ただし変更後に実測してから確定させる
- 仕様書（Issue #2）の「範囲外」に docs/adr/0007 への参照を足す

**未解決の問題**

- 12.2.1 は直ったが、判定結果の文書はまだ更新していない（HSTS と TLS の変更を待って一度に直す）

## 2026-09-15 70項目すべて判定し、3つの検査を通した（Issue #9 完了）

**決めたこと**

- 3つの検査をすべて通した。穴なし（70/70）／終わっている（70=70・漏れも二重も0）／
  再現する（機械の全項目を2回実行して差0行）
- 判定結果の見出しの形を全章で揃えた。検査2が V8 の見出しだけ形が違うことを捕まえた。
  目で数えず機械で数える手順を運用手順に明記した
- まとめを results/siota-1/SUMMARY.md に置いた。不合格4件の直しかたと、判定不能3件の
  解消の道を、非エンジニアが読める形で書いた

- 合格63（うち該当機能なし22）/ 不合格4 / 判定不能3
- 不合格4件: 12.2.1（暗号化なしの通信で中身が返る・最も重い）／3.4.1（HSTS が無い）／
  7.4.1（ログアウト後1時間）／7.2.4（ログインし直しで前が残る）
- 12.2.1 と 3.4.1 は Cloudflare の設定2つ（Always Use HTTPS・HSTS）でコードを変えずに直る
- 判定不能3件: 12.1.1（設定画面1枚）／15.1.1（記録に1行）／15.2.1（15.1.1 待ち）

**次にやること**

- `/code-review` で最後のレビューを通す
- マスターの判断待ち: 不合格4件を直すか。Cloudflare の設定2つは所要数分
- 判定不能3件を片付けたい場合: 設定画面1枚と、記録に1行

**未解決の問題**

- 本番で動いている設定はこちらの目では見ていない（持ち主の操作報告に依る）
- 2本目以降の所要時間は未計測。測るまで数字は出さない

