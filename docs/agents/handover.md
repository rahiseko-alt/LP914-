# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-15 認可4項目の安全確認を通した（Issue #3）

**決めたこと**

- ASVS 公式データの L1 は実測 70 項目。章ごとの内訳も作業一覧の数字と一致した
- 公式データを docs/safety-check/asvs-5.0.0-l1.csv に同梱した。版を固定し、外部に依存させない
- 日本語は外部のドラフト訳に頼らず自分で訳し、原文を併記する（docs/adr/0004）
- 固定表の欄名は CONTEXT.md に合わせて「合格基準」。「合格条件」は使わない
- 命令が何も見つけられなかったときは合格ではなく判定不能とする。黙って合格に倒さない
- `読む` の項目には「見る場所」を命令で絞って書く。検品プログラムが無い項目を赤にする
- siota-1 の認可4項目は 合格4 / 不合格0 / 判定不能0。判定結果の行数が固定表と一致

**次にやること**

- `/implement` で #5〜#8 を進める（互いに独立。どれからでもよい）
- #4（判定不能のヒアリングと書き戻し）は、判定不能が出た章から着手する。
  認可の章では判定不能が0件だったため、まだ実地で試せていない
- 最後に #9 で通し実行と3つの検査

**未解決の問題**

- siota-1 への書き込み権限がまだ無い（#4 で取得する）
- 動いているデータベースの実状は見ていない。移行ファイルの内容だけで判定している。
  Supabase 公式の点検機能を当てるには対象アプリの管理画面への接続が要る

## 2026-09-15 作業を7件に分割して発行（Issue #3〜#9）

**決めたこと**

- Issue #2 を7件の作業に分割した（#3〜#9）。すべて ready-for-agent、#2 の子として登録
- 各件は「表を作る」で終わらせず、その場で siota-1 に当てて判定結果まで出す形にした
- #3 が土台（公式データの取り込み＋認可4項目で全工程を通す＋形式検査プログラム）。待ちなし
- #4 は判定不能の解消を1件通す（siota-1 への書き込み権限の取得を含む）。#3 待ち
- #5〜#8 は残りの章を埋める。互いに独立、いずれも #3 待ち
- #9 は通し実行と3つの検査。#4〜#8 すべて待ち
- 各章の項目数は #3 で公式データから実測し、その値を正とする（70は数え値）

**次にやること**

- `/implement` で #3 から着手する（待ちが無いのは #3 のみ）
- #3 が終わったら #4〜#8 は並行して進められる
- 最後に #9 で判定結果を確定させる

**未解決の問題**

- siota-1 への書き込み権限がまだ無い（#4 の中で取得する）
- ASVS 5.0 の日本語訳を使うか原文から訳すかは #3 で決める

## 2026-09-15 安全確認の仕様書を発行（Issue #2）

**決めたこと**

- 仕様書を GitHub の Issue #2 として発行した。ラベルは ready-for-agent
- テストの境界は1か所のみ「固定表 ＋ 対象アプリ → 判定結果」。確かめるのは3つ
  （表に穴が無いか／判定結果の行数が固定表と一致するか／機械判定が2回で一致するか）
- 表の形式検査だけは小さなプログラムを書いてよい、と利用者の許可を得た
  （アプリを検査する道具ではなく、作った表そのものの検品であるため）
- 該当機能が無い項目は「合格（該当機能なし）」として数え、集計で内訳を分ける。判定は三段階のまま

**次にやること**

- `/to-tickets` で Issue #2 を作業単位に割る
- そのあと `/implement` に進む
- siota-1 への書き込み権限を追加する（実装の前に必要。現在は読み取りのみ）

**未解決の問題**

- ASVS 5.0 の日本語訳（owasp-ja/asvs-ja）はドラフト扱い。使うか原文を訳すかは未決
- 70項目のうち「機械で判定」と「読んで判定」の内訳件数は未確定

## 2026-09-15 安全確認の方針を決定（ASVS L1 の固定表）

**決めたこと**

- 作るのは「70項目の固定表」1枚と運用手順のみ。新しいプログラムは書かない。無料の既存品を組み合わせる
- 土台は OWASP ASVS 5.0.0 の L1（70項目）。項目は事前確定、確認中に増やさない
- 各項目の合格基準も事前に文書で固定し、可能な限り機械が実行する命令に落とす
- 判定は三段階（合格／不合格／判定不能）。根拠はファイル名と行番号つき
- 判定の情報源は3つのみ（機械の出力・コード本体・経緯の記録）。記録とコードが食い違えばコードを正として不合格
- 判定不能が出たら grilling を起動し、業務の事実を掘り下げて聞く。答えは対象アプリ側の記録に書き戻す
- 1本目の対象アプリは rahiseko-alt/siota-1（SALTY DOG）。固定表はこの置き場所に置き、他のアプリにも当てる
- 詳細は CONTEXT.md と docs/adr/0001〜0003

**次にやること**

- `/to-spec` で、この合意を仕様の文書に落とす
- そのあと `/to-tickets` → `/implement` の順に進む
- siota-1 への書き込み権限を追加する（ヒアリングの答えを向こうの記録に書き戻すため。現在は読み取りのみ）

**未解決の問題**

- ASVS には「機械で判定できる項目／人が読むしかない項目」の公式な区分が無い。自前で定義が必要
- ASVS 5.0 の日本語訳（owasp-ja/asvs-ja）はドラフト扱い。これを使うか原文を使うかは未決

## テンプレートから作成

**決めたこと**

- この置き場所はテンプレートから複製したもの。開発の段取り（案内役・開始と終了の儀式・引き継ぎメモ）
  だけが入っており、製品のコードはまだ1行も無い
- 何を作るかはまだ決まっていない

**次にやること**

- `/grill-with-docs` で「何を作るか」を決める。私が質問を重ねるので、答えるだけでよい
- 決まったら `/to-spec` → `/to-tickets` → `/implement` と進む
- `README.md` の冒頭をこのプロジェクトの説明に書き換える（この1件も、書き換えたら消してよい）

**未解決の問題**

- 特になし
